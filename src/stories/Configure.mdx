import { Meta } from "@storybook/blocks";

<Meta title="Getting Started/Initial Setup" />


# ✨ Initial Setup

To use **The Times Graphics Components** in your own React project:

---

## 🔧 Local development setup

To install this package locally, you need:

1. A GitHub personal access token (classic) with `read:packages` permission
2. A `.npmrc` file configured like this:

```ini
@times:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=YOUR_GITHUB_TOKEN
registry=https://registry.npmjs.org/
```


## <span style={{ fontWeight: '600' }}>1. Install the package</span>

```bash
npm install @times/graphics-components
```

> Or if you use Yarn:
```bash
yarn add @times/graphics-components
```

## <span style={{ fontWeight: '600' }}>2. Import and use components</span>

```tsx
import { MediaBlock, ScrollStory } from "@times/graphics-components";
```

Each component is documented with examples and supported props in this Storybook.

## <span style={{ fontWeight: '600' }}>3. Required peer dependencies</span>

Ensure your project has:

- `react` = 19
- `styled-components` ≥ 5
- `framer-motion` (if using animations)


## <span style={{ fontWeight: '600' }}>4. Customization</span>

You can override layout props like:

- `containerWidth`
- `fixedImgHeight`
- `imgBorderRadius`

Check individual stories for full prop support and usage.
